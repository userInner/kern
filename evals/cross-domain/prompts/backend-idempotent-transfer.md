Repair `ledger.go`. Transfers sharing an idempotency key and identical parameters must execute exactly once under concurrency and return the same receipt. Reusing a key with different parameters must return `ErrIdempotencyConflict`. Validate transfers without mutating balances, preserve total funds, and make all public operations race-free. Do not change tests or other files. Run `go test -race ./...`.
