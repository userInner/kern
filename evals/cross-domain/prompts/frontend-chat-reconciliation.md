Repair `messages.js`. `reconcileMessages(current, incoming)` must update existing messages by server `id`, replace optimistic messages when `clientRequestId` matches, preserve the original list position, append genuinely new messages in incoming order, preserve existing fields omitted by a patch, and never mutate either input or nested metadata. Do not change tests or other files. Run `node --test`.
