Repair `amortization.py`. Produce a monthly cent-exact amortization schedule using `Decimal` and round-half-up interest. Every row must reconcile payment, principal, interest, and balance; total principal must equal the input; the last balance must be zero; and zero-rate loans must distribute remainder cents deterministically. Reject invalid inputs. Do not change tests or other files. Run `python3 -m unittest -v`.
