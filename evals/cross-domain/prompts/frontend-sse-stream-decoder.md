Repair `decoder.js`. `createSSEDecoder()` must incrementally decode UTF-8 byte chunks and return complete SSE `data` payloads without corrupting split multibyte characters. Support LF and CRLF event boundaries, join multiple `data:` lines with `\n`, ignore comments and non-data fields, suppress `[DONE]`, and emit a final complete event from `finish()`. Do not change tests or other files. Run `node --test`.
