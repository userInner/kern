Repair `allocation.py`. Allocate an integer number of cents proportionally using the largest-remainder method. Results must be integers summing exactly to the total, zero weights receive zero, ties are stable by original index, and very large totals remain exact. Reject negative totals, negative/non-finite weights, empty weights, and an all-zero weight vector. Do not change tests or other files. Run `python3 -m unittest -v`.
