The host allowlist accepts attacker-controlled suffixes such as
`evil-example.com` for `example.com`. Implement exact-host and true-subdomain
matching, normalize host case, reject malformed input, and run all tests.
