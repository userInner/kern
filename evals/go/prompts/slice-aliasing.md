`Snapshot` promises an independent copy, but callers can mutate the original
state through its result. Repair the ownership boundary with the smallest clear
change, keep the signature, format touched files, and run the repository tests.
