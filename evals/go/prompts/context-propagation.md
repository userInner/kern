Cancellation passed to `Fetch` does not reach the downstream request. Correct
context propagation without inventing a second timeout or changing the client
interface. Add deterministic regression coverage and run all checks.
