`Sorted` unexpectedly mutates its caller's slice. Return sorted independent data
while preserving nil input semantics and the exported signature. Add regression
coverage, format the implementation, and run the tests.
