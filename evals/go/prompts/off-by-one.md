The `LastN` helper fails its test for a request equal to the input length.
Locate the root cause, add any missing boundary coverage, make the smallest
compatible fix, format touched Go files, and run the repository checks. Do not
add dependencies or change the exported signature.
